import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { UmakosPage } from './umakos';

@NgModule({
  declarations: [
    UmakosPage,
  ],
  imports: [
    IonicPageModule.forChild(UmakosPage),
  ],
})
export class UmakosPageModule {}
